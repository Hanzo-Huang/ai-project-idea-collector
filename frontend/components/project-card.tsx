import Link from "next/link";import type { Project } from "@/types";
export function ProjectCard({project}: {project:Project}){return <Link href={`/projects/${project.id}`} className="card group flex min-h-64 flex-col p-5 transition hover:-translate-y-1 hover:border-brand-500/50 hover:shadow-glow"><div className="mb-4 flex items-center justify-between"><span className="badge">{project.project_type}</span><span className="text-xs text-slate-400">{project.source_type}</span></div><h3 className="line-clamp-2 text-lg font-bold group-hover:text-brand-500">{project.title}</h3><p className="mt-2 line-clamp-3 text-sm leading-6 text-slate-500 dark:text-slate-400">{project.summary||project.description||"Analysis in progress..."}</p><div className="mt-4 flex flex-wrap gap-1.5">{project.tags?.slice(0,4).map(t=><span key={t.name} className="rounded-md bg-slate-100 px-2 py-1 text-xs text-slate-500 dark:bg-white/5">#{t.name}</span>)}</div><div className="mt-auto flex items-center justify-between pt-5 text-xs text-slate-400"><span>★ {project.stars.toLocaleString()}</span><span>Idea value <b className="text-brand-500">{project.idea_value.toFixed(1)}</b></span></div></Link>}

