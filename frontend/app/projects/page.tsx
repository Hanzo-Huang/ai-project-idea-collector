"use client";
import { useCallback,useEffect,useState } from "react";import { api } from "@/lib/api";import type { Project } from "@/types";import { ProjectCard } from "@/components/project-card";import { Empty,ErrorState,Loading } from "@/components/page-state";
const types=["","Agent","Model","Application","Dataset","Tool / Framework","Hardware Demo","Tutorial / Blog","AI Event / Trend"];
export default function Projects(){const [projects,setProjects]=useState<Project[]>([]);const [q,setQ]=useState("");const [type,setType]=useState("");const [difficulty,setDifficulty]=useState("");const [source,setSource]=useState("");const [sort,setSort]=useState("newest");const [semantic,setSemantic]=useState(false);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const [total,setTotal]=useState(0);useEffect(()=>{setQ(new URLSearchParams(window.location.search).get("q")||"")},[]);const load=useCallback(()=>{setLoading(true);const p=new URLSearchParams();if(q)p.set("q",q);if(type)p.set("project_type",type);if(difficulty)p.set("difficulty",difficulty);if(source)p.set("source_type",source);p.set("sort",sort);p.set("semantic",String(semantic));api.projects(`?${p}`).then(r=>{setProjects(r.items);setTotal(r.total);setError("")}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[q,type,difficulty,source,sort,semantic]);useEffect(()=>{const timer=setTimeout(load,250);return()=>clearTimeout(timer)},[load]);return <div><div className="mb-6"><h1 className="text-3xl font-bold">Projects</h1><p className="mt-1 text-sm text-slate-500">Explore {total} collected projects and demos.</p></div><div className="card mb-6 p-4"><div className="grid gap-3 lg:grid-cols-[2fr_1fr_1fr_1fr_1fr]"><input className="input" placeholder="Search title, summary, tags..." value={q} onChange={e=>setQ(e.target.value)}/><select className="input" value={type} onChange={e=>setType(e.target.value)}>{types.map(t=><option key={t} value={t}>{t||"All types"}</option>)}</select><select className="input" value={source} onChange={e=>setSource(e.target.value)}><option value="">All sources</option>{["github","huggingface","hackster","youtube","rss","webpage"].map(x=><option key={x}>{x}</option>)}</select><select className="input" value={difficulty} onChange={e=>setDifficulty(e.target.value)}><option value="">Any difficulty</option>{["Beginner","Intermediate","Advanced"].map(x=><option key={x}>{x}</option>)}</select><select className="input" value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest</option><option value="popular">Most popular</option><option value="updated">Recently updated</option><option value="idea_value">Highest idea value</option></select></div><label className="mt-3 flex items-center gap-2 text-xs text-slate-500"><input type="checkbox" checked={semantic} onChange={e=>setSemantic(e.target.checked)}/> Semantic search (when embeddings are configured)</label></div>{error?<ErrorState message={error}/>:loading?<Loading/>:projects.length?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{projects.map(p=><ProjectCard key={p.id} project={p}/>)}</div>:<Empty title="No matching projects" body="Try clearing a filter or collect a new URL."/>}</div>}
